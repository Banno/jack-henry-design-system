---
"@jack-henry/jh-elements": minor
---

[badge] adds `appearance` (negative | neutral), a default slot that anchors the badge to the top-right of slotted content, `label` for accessible text, and a `--jh-badge-color-ring` hook.
