{/*
SPDX-FileCopyrightText: 2026 Jack Henry

SPDX-License-Identifier: Apache-2.0
 */}
import { Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as stories from './layout-h-stack.stories.js';

<Meta title="Primitives/Layout H Stack" of={stories} />

# Layout H Stack

[Guidelines](https://jackhenry.design/pages/components/layout-h-stack/)

<hr />

Layout H Stack is a layout primitive that arranges elements slotted into it in a single horizontal row.
It uses Flexbox under the hood, stacking slotted elements from left to right.

## Usage

Import the `<jh-layout-h-stack>` component:

```javascript
import '@jack-henry/jh-elements/primitives/layout-h-stack/layout-h-stack.js';
```

Place elements in the default slot. Each slotted element becomes a flex item stacked horizontally:

```html
<jh-layout-h-stack gap="100" align="center">
  <jh-button label="Save"></jh-button>
  <jh-button label="Cancel"></jh-button>
</jh-layout-h-stack>
```

## Spacing

`gap` and `padding` both map to the `--jh-dimension-*` token scale, so spacing stays consistent with the rest of the design system.

- `gap` sets the space between slotted elements.
- `padding` sets the inner padding between the container edges and its slotted elements.

Both accept the same values: `0`, `25`, `50`, `100`, `200`, `300`, `400`, `500`, `600`, `700`, `800`, `900`, and `1000`.

```html
<jh-layout-h-stack gap="200" padding="100">
  <!-- slotted elements -->
</jh-layout-h-stack>
```

## Alignment

`align` and `justify` control how slotted elements are positioned:

- `align` sets the cross-axis (vertical) alignment via `align-items`: `start`, `end`, `center`, `stretch`, or `baseline`.
- `justify` sets the main-axis (horizontal) distribution via `justify-content`: `start`, `end`, `center`, `between`, `around`, or `evenly`.

```html
<jh-layout-h-stack align="center" justify="between">
  <!-- slotted elements -->
</jh-layout-h-stack>
```

## Wrapping

By default slotted elements stay on a single line and may overflow the container. Add the boolean `wrap` attribute to let slotted elements reflow onto new lines when they run out of space along the main axis.

```html
<jh-layout-h-stack wrap gap="100">
  <!-- slotted elements -->
</jh-layout-h-stack>
```

## Composition

Layout primitives are designed to compose. Because `<jh-layout-h-stack>` is itself a valid flex child, it nests freely inside `<jh-layout-v-stack>` and other `<jh-layout-h-stack>` containers. See [Layout Composition](?path=/docs/primitives-layout-composition--docs) for common patterns such as toolbars, card layouts, and form rows.

## Component API

For boolean attributes, true indicates that the attribute has been set without a value: `<jh-layout-h-stack wrap></jh-layout-h-stack>`. While false indicates the attribute does not exist in the DOM: `<jh-layout-h-stack></jh-layout-h-stack>`.

<ArgTypes />

## Feedback

Have an idea that will improve this component or have an issue to report? Let us know by submitting 
an issue on [GitHub](https://github.com/Banno/jack-henry-design-system/issues).

Engage with the community on our [GitHub discussion board](https://github.com/Banno/jack-henry-design-system/discussions).
