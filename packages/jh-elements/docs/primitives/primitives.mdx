{/*
SPDX-FileCopyrightText: 2026 Jack Henry

SPDX-License-Identifier: Apache-2.0
*/}

import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Primitives/Primitives" />

# Primitives

Primitives are low-level, unopinionated building blocks. They carry no visual
styling of their own — their job is to arrange other components.

## Layout primitives

- **[Layout Hstack](?path=/docs/primitives-layout-hstack--docs)** — arranges
  its children in a horizontal row (`display: flex; flex-direction: row`).
- **[Layout Vstack](?path=/docs/primitives-layout-vstack--docs)** — arranges
  its children in a vertical column (`display: flex; flex-direction: column`).
- **[Layout Spacer](?path=/docs/primitives-layout-spacer--docs)** — a flexible
  filler that expands to push a stack's children apart. Only has effect inside
  a stack (or any flex parent).

Each stack exposes `gap` and `padding` (mapped to dimension tokens) plus
`align`, `justify`, and `wrap` (mapped to flexbox).

## Seeing them in action

Individually these primitives don't mean much — they're designed to compose,
and each stack is itself a valid flex child, so they nest freely. See
**[Layout Composition](?path=/docs/primitives-layout-composition--docs)** for
real-world patterns: toolbars, nested-stack cards, form rows, and wrapping.
